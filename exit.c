#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void main()
{
	int i =8,x;
	if (i < 6)
	{
		printf("I is less than 6\n");
	}
	else
	{
		x = pow(2,3);
		printf("the value of x is %d\n", x);
	}
	exit(0);
}
